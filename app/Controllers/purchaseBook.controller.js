const {bookPurchase, bookPurchaseDetails} = require('../Model/purchaseBook.model')
const Book = require('./Book.controller')

// exports.CreatePurchaseOrder = async (req, res ) => {
//     try {
//         const existedBook = await Book.
//     } catch (error) {
        
//     }
// }